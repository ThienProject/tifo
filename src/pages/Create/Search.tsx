import { Box } from '@mui/material';
import React from 'react';

const Search = () => {
  return (
    <Box sx={{ height: 100 }} color={'common.black'}>
      {' '}
      Search{' '}
    </Box>
  );
};

export default Search;
