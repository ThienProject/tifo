export const CPath = {
  home: '/',
  login: '/login',
  register: '/register',
  notFound: '*',
  host: process.env.HOST,
  host_public: process.env.HOST + '/medias/',
  baseURL: process.env.HOST + '/api/v1'
};
